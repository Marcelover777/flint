description = "Diagnose Flint hooks, config, pricing, secret scan, MCP shrink, and token-count readiness"
prompt = "Run `node src/commands/flint-doctor.js {{args}}` from the repo root. If user passes `--fix-statusline`, explain it mutates Claude Code settings before running."
