Run safe Flint memory compression.

Command:

```bash
node src/commands/flint-compress.js {{args}}
```

Default recommendation: use `--check --local-only` first. Use `--llm claude-sonnet-4-6` only when user explicitly wants API compression.
