---
description: Show flint lifetime token-savings stats
---
Show flint stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/flint/.flint-history.jsonl`
(or wherever the flint-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
